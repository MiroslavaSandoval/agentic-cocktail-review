# agentic-cocktail-review
A Python application that extracts structured cocktail recipes from PDF documents, validates the results using deterministic rules, stores the results in SQLite, and routes uncertain extractions to human review.
